```tcl
# Verilog 파일 읽기
read_verilog dealloc_use_cnt.v

# 최상위 모듈 설정
hierarchy -check -top dealloc_use_cnt

# 메모리 추론을 위한 합성 단계
proc
opt
fsm
opt
memory -nomap  ;# 플립플롭으로 매핑 방지
memory_map      ;# 배열을 메모리 프리미티브로 매핑
techmap
opt

# 라이브러리 매핑
dfflibmap -liberty NangateOpenCellLibrary_typical.lib
abc -liberty NangateOpenCellLibrary_typical.lib -fast
opt

# 네트리스트 및 보고서 출력
write_verilog -noattr synthesized_dealloc_use_cnt.v
tee -o stat_dealloc_use_cnt.out stat -liberty NangateOpenCellLibrary_typical.lib
```